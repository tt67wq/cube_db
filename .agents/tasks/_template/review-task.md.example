# T-<NN>-<M> review — 独立评审（<对象一句话>）

- **角色**: `review`（评审者 ≠ impl 作者；发现者/评审者回避制见 manifest）
- **评审对象**: 分支 `<branch>` @ `<sha>`（先 `git fetch` 再读，**不 checkout 该分支到你的 worktree**）
- **基线**: main `<sha>`

## 评审步骤

1. `git log --oneline <base>..<branch>` + `git diff <base>..<branch> --stat`
2. 逐行读 diff 与 impl 的 `report.md`（`git show <sha>:report.md`）

## 检查单（按严重度）

- **Blocking 候选**：<按任务类型列——src/ 夹带、断言弱化/删除、时序依赖、report 与 diff 不符、钳制冒充修复等>
- **NB**：<时间盒、注释质量、跨任务自查项等>

## 独立复核要求
<哪些必须评审者本人重跑实证（如确定性/复现对照），哪些可采信 report；注明不重跑全量时的抽查范围>

## 产出

`.agents/tasks/<task-id>/review.md` **commit 到你自己的 worktree 分支**。内容：
verdict（`approve` / `changes-requested`）+ 每条 `file:line` 项 + 被评审的 **SHA**（不是分支名）。
pane 回一行：`T-<NN>-<M> REVIEW <verdict> @<sha>`。
