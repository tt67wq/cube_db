# T-<NN>-<M> — <一句话标题>

- **角色**: `impl`（worker <id>；fixer/reviewer ≠ 发现者，ownership 隔离见 manifest）
- **基线**: main `<sha>`。**从该分支头新开分支** `<branch-name>`（你的 worktree 里先 `git fetch` 同源仓库）
- **来源**: `issues/<issue-file>.md`（先读）

## 背景 / 缺口
<两三行：现状、为什么是问题、已有覆盖是什么（别重复造）>

## 工作
1. <步骤，顺序即 TDD 序：先复现/RED，再修/GREEN>
2. ...

## 硬约束
- **commit 只准落在你的 worktree 路径内；主 checkout 是 conductor 领地**（T-63 流程事故，机械断言见 _template/README.md）
- <ownership 边界：只许动哪些文件/目录，明确点名隔离区>
- <确定性红线：断言不依赖时序 / seed 只喂 key 选择等——如适用>
- <时间盒：场景 ≤~40s、文件 ≤~3min 等——如适用>

## Acceptance（done 定义）
`bash <check.sh 路径> <你的worktree>` exit 0。

## 产出（report.md，唯一作者 = 你）
report.md commit 到你自己的分支：<要求列：根因/思路/证据表/门逐条退出码等>。
pane 回一行：`T-<NN>-<M> GREEN <sha>` 或 `T-<NN>-<M> BLOCKED: <一句话>`。
